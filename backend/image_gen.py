# Calendar PNG generator
# generate_schedule_image(schedule: dict, label: str) -> str  (base64 PNG)
# Renders a Google Calendar-style weekly grid as HTML, screenshots via Playwright
# Color coding: unique color per course, orange for GE, gold (#FFCC00) for double-count GE
# Output: 1100x700px base64-encoded PNG
