# GE course finder
# find_ge_candidates(category: str, constraints: dict) -> list[Section]
# Scrapes USC GE requirement list for a given category letter
# Filters to open sections matching hard constraints
# Flags double-count eligible candidates (appear in 2+ category lists)
